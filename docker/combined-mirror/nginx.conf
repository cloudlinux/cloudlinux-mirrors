server {
    listen 80;
    server_name _;
    root /storage;
    index index.html;

    # ACME challenge for Let's Encrypt
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
    }

    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }

    # Enable directory listing
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # CloudLinux repositories
    location /cloudlinux/ {
        alias /storage/cloudlinux/;
        autoindex on;
    }

    # SWNG repositories
    location /swng/ {
        alias /storage/swng/;
        autoindex on;
    }

}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name __CERTBOT_DOMAIN__;
    root /storage;
    index index.html;

    # SSL Configuration
    ssl_certificate /etc/letsencrypt/live/__CERTBOT_DOMAIN__/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/__CERTBOT_DOMAIN__/privkey.pem;

    # SSL Security Settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Enable directory listing
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    # Logging
    access_log /var/log/nginx/https-access.log;
    error_log /var/log/nginx/https-error.log;

    # CloudLinux repositories
    location /cloudlinux/ {
        alias /storage/cloudlinux/;
        autoindex on;
    }

    # SWNG repositories
    location /swng/ {
        alias /storage/swng/;
        autoindex on;
    }

    # Root location
    location / {
        try_files $uri $uri/ =404;
    }
}
