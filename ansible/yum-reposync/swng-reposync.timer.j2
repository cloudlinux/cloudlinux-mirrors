[Unit]
Description=Run SWNG reposync Every {{ sync_interval_hours }} Hours
Requires={{ service_name }}.service

[Timer]
OnCalendar={{ timer_schedule }}
Persistent=true

[Install]
WantedBy=timers.target
